# Copy to tests/.env (git-ignored) and point at an EMPTY, disposable database.
# The WordPress test suite DROPS AND RECREATES every table in this database.
DMS_TEST_DB_NAME=dms_test
DMS_TEST_DB_USER=dms_test
DMS_TEST_DB_PASSWORD=
DMS_TEST_DB_HOST=localhost
